<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kyotobytes.iubcompanionapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_sqare"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IUBCompanionApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".help_main"></activity>
        <activity android:name=".findmyclassroom" />
        <activity android:name=".library" />
        <activity android:name=".officialwebsite" />
        <activity
            android:name=".iras"
            android:label="iRas V.10" />
        <activity
            android:name=".MainActivity"
            android:label="IUB">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>